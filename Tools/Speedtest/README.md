Buff no longer uses a helper executable for speed testing.

The current implementation runs a native built-in speed test in Buff using
Measurement Lab NDT7 over secure WebSockets.

This folder is no longer required for runtime behavior.
